# Used only for the free public demo build (npm run build:demo).
# The real product build (npm run build, deployed to Vercel) ignores this file.

VITE_DEMO_MODE=true

# GitHub Pages project sites are served at https://<username>.github.io/<repo-name>/
# Update this to match your actual repo name before deploying.
VITE_BASE_PATH=/expense_manager/
